#include <vector>
#include <iostream>
#include <string>
#include <fstream>
#include<ostream>;

void read_list(std::string adress, std::vector <int> *list);
std::vector<int> compare_first_come_par(std::vector<int> &list);
std::vector<int> compare_min_par(std::vector<int>& list);
int single_eleme_comp(std::vector<int>& list);
//void asd(int i,std::vector <int>* list);
